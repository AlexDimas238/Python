# Dicionário
    # Uyiliza index no formato de keys e values
    # Aceita string, integer, float, boolean...


aluno = {
    'nome': 'Ana',
    'idade': 16,
    'Nota_final': 'A',
    'APROVAÇÃO': True,
    'Materias': ['Física', 'Inglês']}

# Atualizando valor da key
    # aluno['nome'] = 'Jose'

    # aluno.update({'nome': 'Jose', 'Nota_final': 'B'})

# Looping
    #for keys, values in aluno.items():
        #print(keys, values)


print(aluno)
